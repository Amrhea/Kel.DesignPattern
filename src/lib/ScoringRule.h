#pragma once

#include <string>

class ScoringRule
{
public:
    static int calculateScore(const std::string& handName, int baseScore) {
        // Placeholder for scoring logic
        return baseScore;
    }
};
